{
  "name": "@onedate/theme",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@react-native-async-storage/async-storage": "^1.23.1",
    "@onedate/config": "0.1.0",
    "@onedate/types": "0.1.0",
    "react": "18.3.1",
    "react-native": "0.76.3",
    "zustand": "^5.0.1"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "test": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  }
}
